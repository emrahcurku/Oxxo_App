<?xml version="1.0" encoding="utf-8" ?>
<TabbedPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:local="clr-namespace:ShopApp"
            x:Class="ShopApp.TappedHome"
   xmlns:android="clr-namespace:Xamarin.Forms.PlatformConfiguration.AndroidSpecific;assembly=Xamarin.Forms.Core" xmlns:local1="clr-namespace:ShopApp.Pages"
            android:TabbedPage.ToolbarPlacement="Bottom"
            SelectedTabColor="Black"
            BarTextColor="Gray"
            NavigationPage.HasNavigationBar="False"
            >
    <NavigationPage  Title="Ana Sayfa" IconImageSource="home.png">
        <x:Arguments>
            <local1:Anasayfa />
        </x:Arguments>
    </NavigationPage>

    <NavigationPage x:Name="katpage" Title="Kategoriler" IconImageSource="category.png">
        <x:Arguments>
            <local1:Categories />
        </x:Arguments>
    </NavigationPage>

    <NavigationPage Title="Favoriler" IconImageSource="favorites.png">
        <x:Arguments>
            <local1:Favoriler />
        </x:Arguments>
    </NavigationPage>

    <NavigationPage Title="Profil" IconImageSource="profile.png">
        <x:Arguments>
            <local1:Profil />
        </x:Arguments>
    </NavigationPage>
</TabbedPage>